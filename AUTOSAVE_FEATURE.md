# whatsmytalent.html 自動儲存功能說明

## ✅ 已完成的功能

### 1. 自動儲存機制
- 使用 `localStorage` 儲存所有使用者資料
- 儲存鍵值：`whatsmytalent_state`
- 每次資料變更時自動觸發儲存

### 2. 儲存的資料內容
```javascript
{
  mode: 'explore' | 'job',           // 選擇的模式
  currentStep: 1 | 2 | 3,            // 當前步驟
  userName: {
    explore: string,                  // 探索模式的姓名
    job: string                       // 職缺模式的姓名
  },
  jobTitle: string,                   // 目標職缺名稱
  explore: {
    strengths: Array<string>,         // 選擇的優勢能力
    growth: Array<string>,            // 選擇的成長能力
    reflections: Object               // 反思描述
  },
  job: {
    selected: Array<string>,          // 選擇的職缺能力
    data: Object                      // 職缺能力詳細資料
  },
  createdAt: number                   // 建立時間戳記
}
```

### 3. 自動恢復功能
- 頁面載入時自動檢測未完成的探索
- 顯示「已自動繼續上次未完成的探索」橫幅
- 30 秒後橫幅自動淡出消失
- 提供「重新開始」按鈕清除所有資料

### 4. 觸發儲存的時機
所有修改狀態的操作都會自動儲存：
- ✅ 選擇模式（探索/職缺）
- ✅ 切換步驟
- ✅ 輸入姓名
- ✅ 輸入職缺標題
- ✅ 標記優勢能力
- ✅ 標記成長能力
- ✅ 標記職缺所需能力
- ✅ 填寫反思描述
- ✅ 填寫職缺能力現況
- ✅ 勾選「已擁有能力」
- ✅ 填寫理想狀態、行動計畫、預計完成時間

## 🎨 UI 元素

### Restore Banner
- 位置：頁面頂部，模式選擇區塊之前
- 樣式：黃色警告框，與 lifevalue.html 一致
- 內容：「🔖 已自動繼續上次未完成的探索。」
- 按鈕：「重新開始」（點擊後清除資料並重新載入頁面）

## 🔧 核心函數

### `saveState()`
- 收集當前所有狀態
- 序列化為 JSON
- 儲存到 localStorage

### `loadState()`
- 從 localStorage 讀取資料
- 解析 JSON
- 返回狀態物件或 null

### `restoreState()`
- 恢復所有狀態變數
- 重建 UI 狀態
- 跳轉到上次的步驟
- 避免觸發額外的 saveState（防止遞迴）

### `clearState()`
- 清除 localStorage 中的資料

### `dismissRestore()`
- 確認後清除所有資料
- 重新載入頁面

## 📱 響應式設計
- 在小螢幕（≤900px）時，橫幅改為垂直排列
- 按鈕全寬顯示
- 文字置中對齊

## 🔒 資料安全
- 所有資料僅儲存在使用者瀏覽器本地
- 不會上傳到伺服器
- 清除瀏覽器資料會同時清除儲存的狀態
- 使用 try-catch 保護 localStorage 操作（避免隱私模式錯誤）

## 🧪 測試建議

### 基本功能測試
1. 選擇「自我探索模式」
2. 填寫姓名
3. 選擇幾個優勢和成長能力
4. 重新整理頁面 → 應該自動恢復到 Step 1
5. 點擊「重新開始」→ 應該清除所有資料

### 跨步驟測試
1. 完成 Step 1 的選擇
2. 進入 Step 2 填寫描述
3. 重新整理頁面 → 應該恢復到 Step 2，且資料完整

### 模式切換測試
1. 在探索模式填寫資料
2. 切換到職缺模式填寫資料
3. 重新整理頁面 → 應該恢復到職缺模式
4. 手動切換回探索模式 → 資料應該保留

### 驗證碼測試
- 驗證碼狀態獨立儲存（14天有效期）
- 自動恢復時應該保持驗證狀態

## 📝 與 lifevalue.html 的差異

### 相同點
- 使用相同的儲存機制
- 相同的 UI 設計
- 相同的自動恢復流程

### 不同點
- **資料結構**：whatsmytalent 有兩種模式（explore/job），資料結構更複雜
- **驗證碼**：whatsmytalent 需要驗證碼才能進入 Step 2+
- **恢復邏輯**：需要根據模式恢復不同的 UI 狀態

## 🎉 完成！

現在 whatsmytalent.html 已經具備完整的自動儲存功能，使用者可以隨時中斷並繼續他們的探索過程，不用擔心資料遺失！
